
console.log("Good Morning , I'm staterd mine exercise about JavaScript data types right now. ");

console.log('Today I did already to the gym trained biceps.');

console.log(`Wassup, How is it goin today ? `);

// Template literals são strings delimitadas por crases (``) e permitem:

// Interpolação de expressões com ${expression}.

// Exemplo de interpolação :

const name = "Guilherme Cosme dos Santos";
console.log(`Wassup, ${name}, how is it goin today?`);
